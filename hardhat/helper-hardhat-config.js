const { ethers } = require("hardhat")

const networkConfig = {
    314159: {
        name: "Calibration",
    },
    314: {
        name: "FilecoinMainnet",
    },
}

module.exports = {
    networkConfig,
}
